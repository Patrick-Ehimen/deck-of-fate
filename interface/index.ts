export interface ButtonProps {
  id: string; // Specify the type for id
  title: string; // Specify the type for title
  rightIcon?: React.ReactNode; // Specify the type for rightIcon
  leftIcon?: React.ReactNode; // Specify the type for leftIcon
  containerClass?: string; // Specify the type for containerClass
}
